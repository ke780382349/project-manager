# 工程约定

## 分包与模块化

- 代码必须按业务模块分包/分文件组织，禁止把多个页面、组件、接口调用堆在同一个文件里。
- 后端（`services/`）：按业务域划包，例如 `auth`、`user`、`project`；通用能力放 `common`、`config`、`web`。新增业务模块时新建独立包，包含自己的实体、仓储、服务、控制器和请求/响应记录类。
- 前端（`web/src/`）：
  - `main.jsx` 只做应用入口渲染；
  - `app.jsx` 放应用外壳与路由切换；
  - `api.js` 放请求封装与令牌常量；
  - `permissions.js` 放权限 ID 常量；
  - `components/` 放可复用组件（侧边栏、头像、下拉框、登录表单等）；
  - `pages/` 放页面级组件，一个页面一个文件；
  - 样式统一在 `styles.css`，按模块分段并加注释分隔。
- 新增页面或组件时直接落到对应目录，不要往已有文件里追加无关内容。

## 其他约定

- 数据库主键统一使用去掉横杠的 32 位 UUID 字符串。
- 接口鉴权使用权限 UUID（`PermissionIds`），前后端保持一致。
- 提交代码前运行：`services` 下 `mvn test`，`web` 下 `npm run build`。
- 未经用户明确允许，不要执行 `git commit` / `git push`。
